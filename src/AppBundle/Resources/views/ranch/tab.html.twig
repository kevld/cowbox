<table class="table table-condensed table-striped table-hover table-bordered pull-left dataTable" id="data-table" aria-describedby="data-table_info">
    <thead>
    <tr role="row">
        <th>
            Id
        </th>
        <th class="text-center">
            Nom
        </th>
        <th class="text-center">
            Importer une liste d'élèves
        </th>
        <th class="text-center">
            Gérer les élèves de ce ranch
        </th>
        <th class="text-center">
            Télécharger les fichiers
        </th>
        <th class="text-center">
            Supprimer
        </th>
    </tr>
    </thead>

    <tbody role="alert" aria-live="polite" aria-relevant="all"><tr class="gradeC odd">
        {% for ranch in ranchs %}
            <tr class="gradeU odd" data-id="{{ ranch.id }}">
                <td class="edit">{{ ranch.id }}</td>
                <td class="edit">{{ ranch.nom }}</td>
                <td class="text-center">
                    <a href="#" class="import"><i class="fa fa-upload text-primary"></i></a>
                </td>
                <td class="text-center">
                    <a href="#" class="ranch"><i class="fa fa-users text-primary"></i></a>
                </td>
                <td class="text-center">
                    <a href="#" class="download"><i class="fa fa-download text-primary"></i></a>
                </td>
                <td class="text-center">
                    <a href="#" data-id="{{ ranch.id }}" class="suppr"><i class="glyphicon glyphicon-remove text-danger"></i></a>
                </td>
            </tr>
        {% else %}
            <tr class="text-center">
                <td colspan="6">Pas de ranch crée</td>
            </tr>
        {% endfor %}
    </tbody>
</table>